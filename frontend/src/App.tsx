import { FC } from "react";
// mui
import Grid from "@mui/material/Unstable_Grid2";

const App: FC = () => {
  return (
    <Grid
      className="App"
      container
      direction="column"
      alignItems="center"
      wrap="nowrap"
    >
      App
    </Grid>
  );
};

export default App;
