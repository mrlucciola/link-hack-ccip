import { FC } from "react";
// components
import RootLayout from "../../layouts/RootLayout";

const Onboarding: FC = () => {
  return <RootLayout>
    
  </RootLayout>;
};

export default Onboarding;
