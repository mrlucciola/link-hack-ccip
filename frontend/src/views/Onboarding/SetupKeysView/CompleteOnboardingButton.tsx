import { FC } from "react";
// mui
import { Button } from "@mui/material";

const CompleteOnboardingButton: FC = () => {
  return <Button>complete onboarding button</Button>;
};

export default CompleteOnboardingButton;
