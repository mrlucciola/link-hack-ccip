import { FC } from "react";
// mui
import Grid from "@mui/material/Unstable_Grid2/Grid2";

const GenerateKeyUtil: FC = () => {
  return <Grid>GenerateKeyUtil</Grid>;
};

export default GenerateKeyUtil;
